export class Hike{
    myHike(name,imgSrc,imgAlt,distance,difficulty,description,directions){
        name = this.name,
        imgSrc = this.imgSrc,
        imgAlt =this.imgAlt,
        distance =this.distance,
        difficulty=this.difficulty,
        description=this.description,
        directions=this.directions
    }
}